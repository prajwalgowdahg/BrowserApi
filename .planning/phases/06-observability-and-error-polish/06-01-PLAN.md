---
phase: 06-observability-and-error-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/middleware/errorHandler.ts
  - src/services/actionLogService.ts
  - src/services/sessionManager.ts
  - tests/errorHandling.test.ts
  - tests/actionLog.test.ts
  - tests/sessionManager.test.ts
autonomous: true
requirements: [ERR-02, LOG-01]

must_haves:
  truths:
    - "When an element is not found, the error JSON response includes a screenshot field with the base64 page screenshot"
    - "ActionLogService stores per-session log entries with timestamp, sessionId, action, status, error, durationMs"
    - "Logs are cleared when a session is destroyed"
  artifacts:
    - path: "src/services/actionLogService.ts"
      provides: "In-memory per-session action log storage with append, getLogs, clear"
      exports: ["ActionLogService", "LogEntry", "actionLogService"]
    - path: "src/middleware/errorHandler.ts"
      provides: "Error handler that detects ElementNotFoundError and attaches screenshot"
      contains: "ElementNotFoundError"
    - path: "src/services/sessionManager.ts"
      provides: "Session cleanup that clears action logs on destroy"
      contains: "actionLogService.clear"
  key_links:
    - from: "src/middleware/errorHandler.ts"
      to: "src/services/cascadeFinder.ts"
      via: "import ElementNotFoundError and check instanceof"
      pattern: "instanceof ElementNotFoundError"
    - from: "src/services/sessionManager.ts"
      to: "src/services/actionLogService.ts"
      via: "import and call clear on destroy"
      pattern: "actionLogService\\.clear"
---

<objective>
Implement ERR-02 (screenshot on element-not-found errors) and create the ActionLogService foundation for LOG-01.

Purpose: Close ERR-02 by enriching error responses with diagnostic screenshots. Build the ActionLogService that Plan 02 will wire into route handlers for LOG-01.

Output: Updated error handler with screenshot enrichment, new ActionLogService module, session cleanup integration, and passing unit tests for all three.
</objective>

<execution_context>
@/Users/prajwalhg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prajwalhg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-observability-and-error-polish/06-RESEARCH.md
@.planning/phases/06-observability-and-error-polish/06-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/services/cascadeFinder.ts:
```typescript
export class ElementNotFoundError extends Error {
  readonly description: string;
  readonly diagnostics: LayerDiagnostic[];
  readonly screenshot: string;
  constructor(description: string, diagnostics: LayerDiagnostic[], screenshot: string);
}
```

From src/middleware/errorHandler.ts (current):
```typescript
export function errorHandler(err: Error, _req: Request, res: Response, _next: NextFunction): void;
// Currently returns { success: false, error: message } -- no screenshot
```

From src/services/sessionManager.ts:
```typescript
export class SessionManager {
  async destroy(sessionId: string): Promise<void>;  // Must add actionLogService.clear() here
}
export const sessionManager: SessionManager;
```

From src/utils/response.ts:
```typescript
interface ErrorResponse {
  success: false;
  error: string;
  screenshot?: string;  // Already typed for optional screenshot
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ERR-02 - Enrich error handler with ElementNotFoundError screenshot</name>
  <files>src/middleware/errorHandler.ts, tests/errorHandling.test.ts</files>
  <behavior>
    - Test: POST to a route that triggers ElementNotFoundError returns response with { success: false, error: "...", screenshot: "base64..." }
    - Test: Regular Error (not ElementNotFoundError) returns response with { success: false, error: "..." } and NO screenshot field
  </behavior>
  <action>
    1. Update tests/errorHandling.test.ts: Add a new describe block for ERR-02 screenshot enrichment. Create a test Express app with a route that calls next(new ElementNotFoundError('test', [], 'fake-screenshot-base64')), verify the response body contains { success: false, error: string containing 'test', screenshot: 'fake-screenshot-base64' }. Also test that a regular Error response does NOT contain a screenshot field.

    2. Update src/middleware/errorHandler.ts: Import ElementNotFoundError from '../services/cascadeFinder.js'. After computing status and message, check `if (err instanceof ElementNotFoundError)` -- if true, respond with { success: false, error: message, screenshot: err.screenshot }. Keep the existing non-screenshot path for all other errors. Keep the exact 4-parameter signature (err, _req, res, _next) for Express 5 compatibility.
  </action>
  <verify>
    <automated>npx vitest run tests/errorHandling.test.ts --reporter=verbose</automated>
  </verify>
  <done>ElementNotFoundError responses include screenshot field; regular errors do not; all existing error handling tests still pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ActionLogService and integrate with session cleanup</name>
  <files>src/services/actionLogService.ts, src/services/sessionManager.ts, tests/actionLog.test.ts, tests/sessionManager.test.ts</files>
  <behavior>
    - Test: actionLogService.append() creates a log entry with auto-generated timestamp and sessionId
    - Test: actionLogService.getLogs() returns entries for a session in order
    - Test: actionLogService.getLogs() returns empty array for unknown session
    - Test: actionLogService.clear() removes all entries for a session
    - Test: SessionManager.destroy() calls actionLogService.clear() for the destroyed session
  </behavior>
  <action>
    1. Create src/services/actionLogService.ts: Export LogEntry interface with fields { timestamp: string, sessionId: string, action: string, status: 'success' | 'fail', error?: string, durationMs?: number }. Export ActionLogService class with private Map<string, LogEntry[]>, methods append(sessionId, entry: Omit<LogEntry, 'timestamp'|'sessionId'>), getLogs(sessionId): LogEntry[], clear(sessionId): void. Export singleton `actionLogService`. Follow the exact pattern from RESEARCH.md Example 2.

    2. Create tests/actionLog.test.ts: Unit tests for ActionLogService -- append creates entries with ISO timestamp and sessionId, getLogs returns ordered entries, getLogs for unknown session returns empty array, clear removes entries. Use a fresh ActionLogService instance per test (do NOT import the singleton for mutation tests).

    3. Update src/services/sessionManager.ts: Import actionLogService from './actionLogService.js'. In the destroy() method, add `actionLogService.clear(sessionId)` BEFORE `this.sessions.delete(sessionId)` so the session ID is still valid when clearing logs.

    4. Update tests/sessionManager.test.ts: Add a test in the delete() or destroy() describe block verifying that actionLogService.clear() is called when a session is destroyed. Import actionLogService, append a log entry for the session, destroy the session, verify getLogs returns empty array.
  </action>
  <verify>
    <automated>npx vitest run tests/actionLog.test.ts tests/sessionManager.test.ts --reporter=verbose</automated>
  </verify>
  <done>ActionLogService stores/retrieves/clears per-session logs; session destruction cleans up logs; all tests pass</done>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose
</verification>

<success_criteria>
1. ElementNotFoundError responses include a screenshot field with the base64 page capture
2. Regular errors (non-ElementNotFoundError) do NOT include a screenshot field
3. ActionLogService stores log entries per session and retrieves them in order
4. Session destruction clears associated action logs
5. All existing tests continue to pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/06-observability-and-error-polish/06-01-SUMMARY.md`
</output>
